# Architecture

> Scaffold — the full architecture write-up lands in the documentation phase.

Planned contents: the line-based wire protocol, component responsibilities
(Name Server, Storage Servers, Client), per-operation data flow
(READ/WRITE/STREAM), the concurrency and sentence-locking model, and the
replication/failover behavior — with topology and sequence diagrams.
